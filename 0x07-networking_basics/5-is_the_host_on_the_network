#!/bin/bash

# Check if argument is provided
if [ -z "$1" ]; then
    echo "Usage: 5-is_the_host_on_the_network {IP_ADDRESS}"
    exit 1
fi

# Proceed with pinging the given IP address
ping -c 5 "$1"
